if (0) {
  // temporary hack to trick cfx into linking the necessary modules
  // into the extension during 'cfx run' and 'cfx xpi'
  require("format");
  require("common");
  require("addonsProvider");
  require("collect");
}
